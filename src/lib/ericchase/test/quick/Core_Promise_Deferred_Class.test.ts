import { describe } from 'bun:test';
import { Core_Promise_Deferred_Class } from '../../Core_Promise_Deferred_Class.js';

/**
 * Should probably write direct test cases for this useful class.
 */

describe(Core_Promise_Deferred_Class.name, () => {});
